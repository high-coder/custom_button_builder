import 'package:flutter/material.dart';

class ButtonAsset {
  String assetPath;
  double? width;
  double? height;
  Color? color;

  ButtonAsset(this.assetPath, {this.width, this.height, this.color});
}
